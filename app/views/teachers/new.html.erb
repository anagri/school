<%= form_for @teacher do |f| %>
  <div><%= f.error_messages %></div>
  <div>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div>
    <%= f.label :classes %>
    <% Clazz.all.each do |clazz| %>
      <div>
        <%= check_box_tag "teacher[clazz_ids][]", clazz.id, @teacher.clazzes.include?(clazz) %>
        <%= clazz.name %>
      </div>
    <% end %>
  </div>
  <div>
    <%= f.submit %>
  </div>
<% end %>